on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Configurar Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Compilar imagen Docker
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/pruebasu:${{ github.sha }} .

      - name: Empujar imagen Docker
        run: docker push ${{ secrets.DOCKER_USERNAME }}/pruebasu:${{ github.sha }}
